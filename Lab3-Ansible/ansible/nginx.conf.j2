server {
    listen 80;
    server_name _;

    # если просто отдаём статику
    root /var/www/html;
    index index.html;

    location / {
        proxy_pass http://python-app:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}